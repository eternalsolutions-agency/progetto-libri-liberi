const SU='https://axudbwobzmmrqpdnbamp.supabase.co',SK='sb_publishable_JHJBoonYn5HFxERyv-RWKA_oKpYsV7W';const base=()=> 'https://api-m.sandbox.paypal.com';async function tok(){const i=process.env.PAYPAL_CLIENT_ID,s=process.env.PAYPAL_CLIENT_SECRET,r=await fetch(base()+'/v1/oauth2/token',{method:'POST',headers:{Authorization:`Basic ${Buffer.from(`${i}:${s}`).toString('base64')}`,'Content-Type':'application/x-www-form-urlencoded'},body:'grant_type=client_credentials'}),d=await r.json();if(!r.ok)throw Error(d.error_description||'PayPal auth');return d.access_token}
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({ok:false});try{if((process.env.PAYPAL_ENV||'sandbox')!=='sandbox')throw Error('Solo Sandbox');const{subscription_id,tipologia,piano,importo,plan_id}=req.body||{};if(!subscription_id||!plan_id)throw Error('Dati mancanti');const t=await tok(),r=await fetch(`${base()}/v1/billing/subscriptions/${encodeURIComponent(subscription_id)}`,{headers:{Authorization:`Bearer ${t}`}}),sub=await r.json();if(!r.ok)throw Error(sub.message||'Verifica PayPal fallita');if(sub.plan_id!==plan_id)throw Error('Piano PayPal non corrispondente');if(!['ACTIVE','APPROVAL_PENDING','APPROVED'].includes(sub.status))throw Error('Stato PayPal non valido: '+sub.status);const rr=await fetch(`${SU}/rest/v1/pagamenti`,{method:'POST',headers:{apikey:SK,Authorization:`Bearer ${SK}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify({email:sub.subscriber?.email_address||null,tipologia,piano,importo:Number(importo),provider:'paypal',provider_subscription_id:subscription_id,stato:sub.status==='ACTIVE'?'attivo':'in_attesa',data_inizio:new Date().toISOString()})});if(!rr.ok)throw Error('Supabase: '+await rr.text());return res.status(200).json({ok:true,subscription_id,status:sub.status})}catch(e){return res.status(500).json({ok:false,message:e.message})}};